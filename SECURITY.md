# 安全与公开范围

不要在issue上传密码、token、登录state、私有URL query、完整HAR、数据库或原始聊天截图。只发版本、脱敏错误、聚合计时和复现步骤。网络变更需要主机管理员授权，默认预览不是已安装。

本仓库不读取凭据，也不提供跳过登录或关闭CSP的工具。Node测试采用内存模拟响应；Python规则测试采用模拟命令，不对CI主机防火墙动手。

发现安全问题时，先在不包含利用凭据/个人数据的issue描述影响范围，敏感细节不要公开粘贴。未知版本不代表安全，只代表未测试，工具应拒绝修改。
